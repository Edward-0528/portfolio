import React from 'react';

/**
 * MinimalNetworkBackground — now a no-op component.
 * The heavy canvas particle animation has been removed for performance.
 * Background effects are handled via CSS gradients in the new design.
 */
const MinimalNetworkBackground = () => {
  return null;
};

export default MinimalNetworkBackground;
